##Para que este servicio se registre en Eureka:
eureka:
  client:
    service-url:
      defaultZone: ${eureka_client_url}

##Info para Spring Actuator:
info:
  app:
    name: config-service
    description: Configuration Service created for development environment.
    version: 1.0.0