server:
  port: 8082

##Para que este servicio se registre en Eureka:
spring:
  eureka:
    client:
      service-url:
        defaultZone: ${eureka_client_url}

##Info para Spring Actuator:
info:
  app:
    name: game-service
    description: Game Service created for development environment.
    version: 1.0.0